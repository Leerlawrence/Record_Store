var Record = function(artist, title, price){
  this.artist = artist;
  this.title = title;
  this.price = price;


 addRecord: function (artist, title, price){    
          var record = new Record(artist, title, price);
          //'people.push(p);
     }

  };

module.exports = Record;

// var Baddy = function(name){
//   this.name=name;
// }

